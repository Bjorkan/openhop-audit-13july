# BUG-040 — APP_START overwrites protocol version from a reserved byte and accepts short frames

[← Bug list](../README.md#bug-list)

| Field | Value |
|---|---|
| Severity | **Medium** |
| Area | Companion session negotiation |
| Affected components | OpenHop Core |
| Status | Confirmed from the supplied source snapshots |

## TL;DR

MeshCore negotiates app_target_ver only through DEVICE_QUERY. APP_START has seven reserved bytes and requires the full reserved prefix. OpenHop treats APP_START data[0] as a version and accepts even an empty/one-byte command. To fix it, leave the negotiated version untouched in APP_START and reject frames shorter than seven data bytes. Keep DEVICE_QUERY as the sole version-setting command.

**Current status: ✅ Fully fixed.** APP_START now requires all seven reserved bytes and no longer changes the negotiated app target version; version negotiation remains confined to DEVICE_QUERY. The corrected handler is in [commands_device.py](https://github.com/openhop-dev/openhop_core/blob/fix/all-the-things-core/src/openhop_core/companion/frame_server/commands_device.py#L35-L44). This fix was introduced in [`42b93c8` — `fix(companion): stop treating APP_START prefix as a protocol version`](https://github.com/openhop-dev/openhop_core/commit/42b93c8).

## What happens

MeshCore negotiates app_target_ver only through DEVICE_QUERY. APP_START has seven reserved bytes and requires the full reserved prefix. OpenHop treats APP_START data[0] as a version and accepts even an empty/one-byte command.

## How official MeshCore handles it

CMD_DEVICE_QUERY with len >= 2 stores cmd_frame[1]. CMD_APP_START requires total len >= 8, treats bytes 1–7 as reserved, and does not modify app_target_ver.

## How the OpenHop stack handles it

**OpenHop Core:** _cmd_app_start assigns _app_target_ver = data[0] whenever present and has no minimum seven-byte validation.

## What needs to change

Leave the negotiated version untouched in APP_START and reject frames shorter than seven data bytes. Keep DEVICE_QUERY as the sole version-setting command.

## Suggested code change

**Important:** The linked code was generated by an LLM. It is not reviewed, tested, or ready to apply directly. It only illustrates the approximate files, control flow, and code changes that may be required. A maintainer must verify the behavior against MeshCore, rewrite it as needed, add tests, and review interactions with other fixes.

- **OpenHop Core:** `src/openhop_core/companion/frame_server/commands_device.py`

[Open the suggested patch](../patches/BUG-040.patch)

## Source links

These links point to the branches reviewed for this audit. Line numbers can move after later commits.

| Project | Why it matters | Source |
|---|---|---|
| MeshCore | Reference | [`examples/companion_radio/MyMesh.cpp` L1008–L1070](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.cpp#L1008-L1070) |
| OpenHop Core | Affected implementation | [`src/openhop_core/companion/frame_server/commands_device.py` L37–L70](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/companion/frame_server/commands_device.py#L37-L70) |

## Relevant source excerpts

The excerpts are collapsed to keep the report easy to scan.

<details>
<summary><strong>MeshCore</strong> — <code>examples/companion_radio/MyMesh.cpp</code> (L1008–L1017, L1021–L1037)</summary>

[Open the cited range on GitHub](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.cpp#L1008-L1070)

```cpp
1008 | void MyMesh::handleCmdFrame(size_t len) {
1009 |   if (cmd_frame[0] == CMD_DEVICE_QUERY && len >= 2) { // sent when app establishes connection
1010 |     app_target_ver = cmd_frame[1];                    // which version of protocol does app understand
1011 | 
1012 |     int i = 0;
1013 |     out_frame[i++] = RESP_CODE_DEVICE_INFO;
1014 |     out_frame[i++] = FIRMWARE_VER_CODE;
1015 |     out_frame[i++] = MAX_CONTACTS / 2;   // v3+
1016 |     out_frame[i++] = MAX_GROUP_CHANNELS; // v3+
1017 |     memcpy(&out_frame[i], &_prefs.ble_pin, 4);
…
1021 |     i += 12;
1022 |     StrHelper::strzcpy((char *)&out_frame[i], board.getManufacturerName(), 40);
1023 |     i += 40;
1024 |     StrHelper::strzcpy((char *)&out_frame[i], FIRMWARE_VERSION, 20);
1025 |     i += 20;
1026 |     out_frame[i++] = _prefs.client_repeat;   // v9+
1027 |     out_frame[i++] = _prefs.path_hash_mode;  // v10+
1028 |     _serial->writeFrame(out_frame, i);
1029 |   } else if (cmd_frame[0] == CMD_APP_START &&
1030 |              len >= 8) { // sent when app establishes connection, respond with node ID
1031 |     //  cmd_frame[1..7]  reserved future
1032 |     char *app_name = (char *)&cmd_frame[8];
1033 |     cmd_frame[len] = 0; // make app_name null terminated
1034 |     MESH_DEBUG_PRINTLN("App %s connected", app_name);
1035 | 
1036 |     _iter_started = false; // stop any left-over ContactsIterator
1037 |     int i = 0;
```

</details>

<details>
<summary><strong>OpenHop Core</strong> — <code>src/openhop_core/companion/frame_server/commands_device.py</code> (L37–L70)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/companion/frame_server/commands_device.py#L37-L70)

```python
37 |     async def _cmd_app_start(self, data: bytes) -> None:
38 |         if len(data) >= 1:
39 |             self._app_target_ver = data[0]
40 |         prefs = self.bridge.get_self_info()
41 |         pubkey = self.bridge.get_public_key()
42 |         name = prefs.node_name.encode("utf-8", errors="replace")
43 |         lat = int(getattr(prefs, "latitude", 0) * 1e6)
44 |         lon = int(getattr(prefs, "longitude", 0) * 1e6)
45 |         frame = (
46 |             bytes([RESP_CODE_SELF_INFO, ADV_TYPE_CHAT, prefs.tx_power_dbm, 22])
47 |             + pubkey
48 |             + struct.pack("<ii", lat, lon)
49 |             + bytes(
50 |                 [
51 |                     getattr(prefs, "multi_acks", 0),
52 |                     getattr(prefs, "advert_loc_policy", 0),
53 |                 ]
54 |             )
55 |             + bytes(
56 |                 [
57 |                     getattr(prefs, "telemetry_mode_base", 0)
58 |                     | (getattr(prefs, "telemetry_mode_location", 0) << 2)
59 |                 ]
60 |             )
61 |             + bytes([getattr(prefs, "manual_add_contacts", 0)])
62 |             + struct.pack(
63 |                 "<II",
64 |                 prefs.frequency_hz // 1000,
65 |                 prefs.bandwidth_hz,
66 |             )
67 |             + bytes([prefs.spreading_factor, prefs.coding_rate])
68 |             + name
69 |         )
70 |         self._write_frame(frame)
```

</details>
