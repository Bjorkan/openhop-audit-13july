# BUG-043 — The implemented companion signing pipeline is unreachable

[← Bug list](../README.md#bug-list)

| Field | Value |
|---|---|
| Severity | **Medium** |
| Area | Companion signing |
| Affected components | OpenHop Core |
| Status | Confirmed from the supplied source snapshots |

## TL;DR

OpenHop implements sign_start, sign_data, and sign_finish in the bridge, and defines the command constants, but CompanionFrameServer does not register handlers for CMD_SIGN_START, CMD_SIGN_DATA, or CMD_SIGN_FINISH. To fix it, add frame handlers and registry entries with MeshCore's response layouts, session lifecycle, overflow errors, cleanup, and 8 KiB limit.

**Current status: 🔴 Not fixed.** The bridge methods and command constants exist, but CompanionFrameServer still has no CMD_SIGN_START, CMD_SIGN_DATA, or CMD_SIGN_FINISH entries in its command registry and no frame handlers implementing their response lifecycle. The gap remains in [the registry](https://github.com/openhop-dev/openhop_core/blob/fix/all-the-things-core/src/openhop_core/companion/frame_server/server.py#L158-L210).

## What happens

OpenHop implements sign_start, sign_data, and sign_finish in the bridge, and defines the command constants, but CompanionFrameServer does not register handlers for CMD_SIGN_START, CMD_SIGN_DATA, or CMD_SIGN_FINISH.

## How official MeshCore handles it

The companion allocates an 8 KiB signing session, returns its capacity, appends chunks with bounds checks, and returns the signature on finish.

## How the OpenHop stack handles it

**OpenHop Core:** A client sending any of the three commands receives unsupported-command handling; the bridge methods cannot be invoked through the protocol.

## What needs to change

Add frame handlers and registry entries with MeshCore's response layouts, session lifecycle, overflow errors, cleanup, and 8 KiB limit.

## Suggested code change

**Important:** The linked code was generated by an LLM. It is not reviewed, tested, or ready to apply directly. It only illustrates the approximate files, control flow, and code changes that may be required. A maintainer must verify the behavior against MeshCore, rewrite it as needed, add tests, and review interactions with other fixes.

- **OpenHop Core:** `src/openhop_core/companion/base_config.py`
- **OpenHop Core:** `src/openhop_core/companion/frame_server/server.py`

[Open the suggested patch](../patches/BUG-043.patch)

## Source links

These links point to the branches reviewed for this audit. Line numbers can move after later commits.

| Project | Why it matters | Source |
|---|---|---|
| MeshCore | Reference | [`examples/companion_radio/MyMesh.cpp` L1712–L1743](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.cpp#L1712-L1743) |
| OpenHop Core | Affected implementation | [`src/openhop_core/companion/base_config.py` L145–L175](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/companion/base_config.py#L145-L175) |
| OpenHop Core | Affected implementation | [`src/openhop_core/companion/frame_server/server.py` L152–L204](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/companion/frame_server/server.py#L152-L204) |

## Relevant source excerpts

The excerpts are collapsed to keep the report easy to scan.

<details>
<summary><strong>MeshCore</strong> — <code>examples/companion_radio/MyMesh.cpp</code> (L1712–L1743)</summary>

[Open the cited range on GitHub](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.cpp#L1712-L1743)

```cpp
1712 |   } else if (cmd_frame[0] == CMD_SIGN_START) {
1713 |     out_frame[0] = RESP_CODE_SIGN_START;
1714 |     out_frame[1] = 0; // reserved
1715 |     uint32_t len = MAX_SIGN_DATA_LEN;
1716 |     memcpy(&out_frame[2], &len, 4);
1717 |     _serial->writeFrame(out_frame, 6);
1718 | 
1719 |     if (sign_data) {
1720 |       free(sign_data);
1721 |     }
1722 |     sign_data = (uint8_t *)malloc(MAX_SIGN_DATA_LEN);
1723 |     sign_data_len = 0;
1724 |   } else if (cmd_frame[0] == CMD_SIGN_DATA && len > 1) {
1725 |     if (sign_data == NULL || sign_data_len + (len - 1) > MAX_SIGN_DATA_LEN) {
1726 |       writeErrFrame(sign_data == NULL ? ERR_CODE_BAD_STATE : ERR_CODE_TABLE_FULL); // error: too long
1727 |     } else {
1728 |       memcpy(&sign_data[sign_data_len], &cmd_frame[1], len - 1);
1729 |       sign_data_len += (len - 1);
1730 |       writeOKFrame();
1731 |     }
1732 |   } else if (cmd_frame[0] == CMD_SIGN_FINISH) {
1733 |     if (sign_data) {
1734 |       self_id.sign(&out_frame[1], sign_data, sign_data_len);
1735 | 
1736 |       free(sign_data); // don't need sign_data now
1737 |       sign_data = NULL;
1738 | 
1739 |       out_frame[0] = RESP_CODE_SIGNATURE;
1740 |       _serial->writeFrame(out_frame, 1 + SIGNATURE_SIZE);
1741 |     } else {
1742 |       writeErrFrame(ERR_CODE_BAD_STATE);
1743 |     }
```

</details>

<details>
<summary><strong>OpenHop Core</strong> — <code>src/openhop_core/companion/base_config.py</code> (L145–L175)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/companion/base_config.py#L145-L175)

```python
145 |     # -------------------------------------------------------------------------
146 |     # Signing Pipeline
147 |     # -------------------------------------------------------------------------
148 | 
149 |     def sign_start(self) -> int:
150 |         """Begin a signing session; returns the maximum sign buffer size."""
151 |         self._sign_buffer = bytearray()
152 |         return MAX_SIGN_DATA_SIZE
153 | 
154 |     def sign_data(self, data: bytes) -> bool:
155 |         """Append data to the signing buffer."""
156 |         if self._sign_buffer is None:
157 |             logger.warning("sign_data called without sign_start")
158 |             return False
159 |         if len(self._sign_buffer) + len(data) > MAX_SIGN_DATA_SIZE:
160 |             logger.warning("Sign data would overflow buffer")
161 |             return False
162 |         self._sign_buffer.extend(data)
163 |         return True
164 | 
165 |     def sign_finish(self) -> Optional[bytes]:
166 |         if self._sign_buffer is None:
167 |             logger.warning("sign_finish called without sign_start")
168 |             return None
169 |         try:
170 |             return self._identity.sign(bytes(self._sign_buffer))
171 |         except Exception as e:
172 |             logger.error("Signing error: %s", e)
173 |             return None
174 |         finally:
175 |             self._sign_buffer = None
```

</details>

<details>
<summary><strong>OpenHop Core</strong> — <code>src/openhop_core/companion/frame_server/server.py</code> (L172–L204)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/companion/frame_server/server.py#L152-L204)

```python
172 |             CMD_GET_STATS: self._cmd_get_stats,
173 |             CMD_GET_ADVERT_PATH: self._cmd_get_advert_path,
174 |             CMD_IMPORT_CONTACT: self._cmd_import_contact,
175 |             CMD_GET_CHANNEL: self._cmd_get_channel,
176 |             CMD_SET_CHANNEL: self._cmd_set_channel,
177 |             CMD_SEND_BINARY_REQ: self._cmd_send_binary_req,
178 |             CMD_SEND_ANON_REQ: self._cmd_send_anon_req,
179 |             CMD_SEND_PATH_DISCOVERY_REQ: self._cmd_send_path_discovery_req,
180 |             CMD_SEND_CONTROL_DATA: self._cmd_send_control_data,
181 |             CMD_SEND_TRACE_PATH: self._cmd_send_trace_path,
182 |             CMD_SET_FLOOD_SCOPE: self._cmd_set_flood_scope,
183 |             CMD_SET_DEFAULT_FLOOD_SCOPE: self._cmd_set_default_flood_scope,
184 |             CMD_GET_DEFAULT_FLOOD_SCOPE: self._cmd_get_default_flood_scope,
185 |             CMD_GET_DEVICE_TIME: self._cmd_get_device_time,
186 |             CMD_SET_DEVICE_TIME: self._cmd_set_device_time,
187 |             CMD_SET_RADIO_PARAMS: self._cmd_set_radio_params,
188 |             CMD_SET_RADIO_TX_POWER: self._cmd_set_tx_power,
189 |             CMD_SHARE_CONTACT: self._cmd_share_contact,
190 |             CMD_EXPORT_CONTACT: self._cmd_export_contact,
191 |             CMD_EXPORT_PRIVATE_KEY: self._cmd_export_private_key,
192 |             CMD_IMPORT_PRIVATE_KEY: self._cmd_import_private_key,
193 |             CMD_SET_TUNING_PARAMS: self._cmd_set_tuning_params,
194 |             CMD_LOGOUT: self._cmd_logout,
195 |             CMD_GET_CUSTOM_VARS: self._cmd_get_custom_vars,
196 |             CMD_SET_CUSTOM_VAR: self._cmd_set_custom_var,
197 |             CMD_SET_AUTOADD_CONFIG: self._cmd_set_autoadd_config,
198 |             CMD_GET_AUTOADD_CONFIG: self._cmd_get_autoadd_config,
199 |             CMD_SET_OTHER_PARAMS: self._cmd_set_other_params,
200 |             CMD_SEND_RAW_DATA: self._cmd_send_raw_data,
201 |             CMD_SEND_RAW_PACKET: self._cmd_send_raw_packet,
202 |             CMD_SET_PATH_HASH_MODE: self._cmd_set_path_hash_mode,
203 |             CMD_GET_ALLOWED_REPEAT_FREQ: self._cmd_get_allowed_repeat_freq,
204 |         }
```

</details>
