# BUG-065 — Reserved payload version 1 is accepted as a supported wire format

[← Active finding list](../README.md#active-findings)

| Field | Value |
|---|---|
| Severity | **Medium** |
| Area | Packet parsing |
| Affected components | OpenHop Core |
| Status | Confirmed from the supplied source snapshots |

## TL;DR

OpenHop accepts payload-version bits 0 and 1 even though the official firmware supports only version 0. Version 1 is explicitly reserved for a future layout that may change address-hash and MAC widths, so decoding it with the version-0 layout can misparse future or malformed packets.

**Current status: 🔴 Not fixed.** Rechecked against latest supplied Core head [`abe1d85`](https://github.com/openhop-dev/openhop_core/commit/abe1d857734d5d1f549ada27af6f95c7e5e53e95) and Repeater head [`b62960f`](https://github.com/openhop-dev/openhop_repeater/commit/b62960fa3041447bedadb2131d892a6565d7e519), as applicable. The three new Core commits and two new Repeater commits do not remove this mismatch; the complete regression suites and focused changed-path review found no contrary behavior.

## What happens

OpenHop accepts payload-version bits 0 and 1 even though the official firmware supports only version 0. Version 1 is explicitly reserved for a future layout that may change address-hash and MAC widths, so decoding it with the version-0 layout can misparse future or malformed packets.

## How official MeshCore handles it

The official dispatcher rejects any packet whose payload version is greater than `PAYLOAD_VER_1` (numeric value 0).

## How the OpenHop stack handles it

`MAX_SUPPORTED_PAYLOAD_VERSION` is set to `PAYLOAD_VER_2` (numeric value 1), and `Packet.read_from()` rejects only larger values. Version 1 is therefore decoded as though it had the current layout.

## What needs to change

Limit parsing to numeric version 0 until a complete version-1 layout is implemented. Add a focused deserialization test for each reserved version.

## Suggested code change

**Important:** The linked code was generated by an LLM. It is not reviewed, tested, or ready to apply directly. It only illustrates the approximate files, control flow, and code changes that may be required. A maintainer must verify the behavior against MeshCore, rewrite it as needed, add tests, and review interactions with other fixes.

[Open the suggested patch](../patches/BUG-065.patch)

## Source links

These links identify the code ranges used for this finding. The local ZIP snapshots listed in `AUDIT-SNAPSHOT.txt` are the audit source of truth.

| Project | Why it matters | Source |
|---|---|---|
| Core | Affected implementation | [`src/openhop_core/protocol/constants.py` L38–L46](https://github.com/openhop-dev/openhop_core/blob/9ea7269a7e7e903fe433b1f952a4026fe3dcc81b/src/openhop_core/protocol/constants.py#L38-L46) |
| Core | Affected implementation | [`src/openhop_core/protocol/packet.py` L443–L451](https://github.com/openhop-dev/openhop_core/blob/9ea7269a7e7e903fe433b1f952a4026fe3dcc81b/src/openhop_core/protocol/packet.py#L443-L451) |
| MeshCore | Official reference | [`src/Dispatcher.cpp` L148–L155](https://github.com/meshcore-dev/MeshCore/blob/main/src/Dispatcher.cpp#L148-L155) |

## Relevant source excerpts

<details>
<summary><strong>Core</strong> — <code>src/openhop_core/protocol/constants.py</code> (L38–L46)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_core/blob/9ea7269a7e7e903fe433b1f952a4026fe3dcc81b/src/openhop_core/protocol/constants.py#L38-L46)

```python
  38 | # ---------------------------------------------------------------------------
  39 | # Payload version values (2 bits)
  40 | # ---------------------------------------------------------------------------
  41 | PAYLOAD_VER_1 = 0x00  # Currently supported
  42 | PAYLOAD_VER_2 = 0x01  # Reserved for future use
  43 | PAYLOAD_VER_3 = 0x02  # Reserved for future use
  44 | PAYLOAD_VER_4 = 0x03  # Reserved for future use
  45 | MAX_SUPPORTED_PAYLOAD_VERSION = PAYLOAD_VER_2  # Accept versions 0-1
  46 | 
```

</details>
<details>
<summary><strong>Core</strong> — <code>src/openhop_core/protocol/packet.py</code> (L443–L451)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_core/blob/9ea7269a7e7e903fe433b1f952a4026fe3dcc81b/src/openhop_core/protocol/packet.py#L443-L451)

```python
 443 |         idx, data_len = 0, len(data)
 444 |         self.header = data[idx]
 445 |         idx += 1
 446 | 
 447 |         # Validate packet version (must match C++ supported versions)
 448 |         version = self.get_payload_ver()
 449 |         if version > MAX_SUPPORTED_PAYLOAD_VERSION:
 450 |             raise ValueError(f"Unsupported packet version: {version}")
 451 | 
```

</details>
<details>
<summary><strong>MeshCore</strong> — <code>src/Dispatcher.cpp</code> (L148–L155)</summary>

[Open the cited range on GitHub](https://github.com/meshcore-dev/MeshCore/blob/main/src/Dispatcher.cpp#L148-L155)

```cpp
 148 | bool Dispatcher::tryParsePacket(Packet* pkt, const uint8_t* raw, int len) {
 149 |   int i = 0;
 150 | 
 151 |   pkt->header = raw[i++];
 152 |   if (pkt->getPayloadVer() > PAYLOAD_VER_1) {
 153 |     MESH_DEBUG_PRINTLN("%s Dispatcher::checkRecv(): unsupported packet version", getLogDateTime());
 154 |     return false;
 155 |   }
```

</details>
