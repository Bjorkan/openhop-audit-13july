# BUG-057 — PathHelper treats encoded path_len as a byte count

[← Bug list](../README.md#bug-list)

| Field | Value |
|---|---|
| Severity | **High** |
| Area | Repeater PATH parsing |
| Affected components | OpenHop Repeater |
| Status | Confirmed from the supplied source snapshots |

## TL;DR

The first decrypted PATH byte stores hash width in bits 6-7 and hop count in bits 0-5. PathHelper uses the entire encoded byte directly as the slice length and truncation requirement. To fix it, use `PathUtils.is_valid_path_len` and `get_path_byte_len`, slice exactly that many bytes, and store encoded `path_len` separately from outbound path bytes. Test one-, two-, and three-byte hashes, including zero-valued path bytes.

**Current status: ✅ Fully fixed.** PathHelper now validates the encoded path_len, derives the actual byte count with PathUtils.get_path_byte_len, copies exactly that many bytes, and stores the original encoded byte separately. The corrected parse is in [path.py](https://github.com/openhop-dev/openhop_repeater/blob/fix/all-the-things/repeater/handler_helpers/path.py#L81-L119). This fix was introduced in [`62ad742` — `fix(router): consume PATH and RESPONSE only after MAC authentication`](https://github.com/openhop-dev/openhop_repeater/commit/62ad742).

## What happens

The first decrypted PATH byte stores hash width in bits 6-7 and hop count in bits 0-5. PathHelper uses the entire encoded byte directly as the slice length and truncation requirement.

## How official MeshCore handles it

MeshCore calculates `hash_size = (path_len >> 6) + 1` and `hash_count = path_len & 63`, then copies `hash_size * hash_count` bytes while preserving the encoded descriptor.

## How the OpenHop stack handles it

**OpenHop Repeater:** A two-byte path encoded as `0x42` with four path bytes is treated as requiring 66 bytes and is rejected. Path learning effectively works only for one-byte hashes.

## What needs to change

Use `PathUtils.is_valid_path_len` and `get_path_byte_len`, slice exactly that many bytes, and store encoded `path_len` separately from outbound path bytes. Test one-, two-, and three-byte hashes, including zero-valued path bytes.

## Suggested code change

**Important:** The linked code was generated by an LLM. It is not reviewed, tested, or ready to apply directly. It only illustrates the approximate files, control flow, and code changes that may be required. A maintainer must verify the behavior against MeshCore, rewrite it as needed, add tests, and review interactions with other fixes.

- **OpenHop Repeater:** `repeater/handler_helpers/path.py`

[Open the suggested patch](../patches/BUG-057.patch)

## Source links

These links point to the branches reviewed for this audit. Line numbers can move after later commits.

| Project | Why it matters | Source |
|---|---|---|
| MeshCore | Reference | [`src/Mesh.cpp` L150–L168](https://github.com/meshcore-dev/MeshCore/blob/main/src/Mesh.cpp#L150-L168) |
| MeshCore | Reference | [`src/Packet.h` L72–L85](https://github.com/meshcore-dev/MeshCore/blob/main/src/Packet.h#L72-L85) |
| OpenHop Repeater | Affected implementation | [`repeater/handler_helpers/path.py` L55–L82](https://github.com/openhop-dev/openhop_repeater/blob/dev/repeater/handler_helpers/path.py#L55-L82) |

## Relevant source excerpts

The excerpts are collapsed to keep the report easy to scan.

<details>
<summary><strong>MeshCore</strong> — <code>src/Mesh.cpp</code> (L150–L168)</summary>

[Open the cited range on GitHub](https://github.com/meshcore-dev/MeshCore/blob/main/src/Mesh.cpp#L150-L168)

```cpp
150 | 
151 |             // decrypt, checking MAC is valid
152 |             uint8_t data[MAX_PACKET_PAYLOAD];
153 |             int len = Utils::MACThenDecrypt(secret, data, macAndData, pkt->payload_len - i);
154 |             if (len > 0) {  // success!
155 |               if (pkt->getPayloadType() == PAYLOAD_TYPE_PATH) {
156 |                 int k = 0;
157 |                 uint8_t path_len = data[k++];
158 |                 uint8_t hash_size = (path_len >> 6) + 1;
159 |                 uint8_t hash_count = path_len & 63;
160 |                 uint8_t* path = &data[k]; k += hash_size*hash_count;
161 |                 uint8_t extra_type = data[k++] & 0x0F;   // upper 4 bits reserved for future use
162 |                 uint8_t* extra = &data[k];
163 |                 uint8_t extra_len = len - k;   // remainder of packet (may be padded with zeroes!)
164 |                 if (onPeerPathRecv(pkt, j, secret, path, path_len, extra_type, extra, extra_len)) {
165 |                   if (pkt->isRouteFlood()) {
166 |                     // send a reciprocal return path to sender, but send DIRECTLY!
167 |                     mesh::Packet* rpath = createPathReturn(&src_hash, secret, pkt->path, pkt->path_len, 0, NULL, 0);
168 |                     if (rpath) sendDirect(rpath, path, path_len, 500);
```

</details>

<details>
<summary><strong>MeshCore</strong> — <code>src/Packet.h</code> (L72–L85)</summary>

[Open the cited range on GitHub](https://github.com/meshcore-dev/MeshCore/blob/main/src/Packet.h#L72-L85)

```cpp
72 |   uint8_t getPayloadType() const { return (header >> PH_TYPE_SHIFT) & PH_TYPE_MASK; }
73 | 
74 |   /**
75 |    * \returns  one of PAYLOAD_VER_ values
76 |    */
77 |   uint8_t getPayloadVer() const { return (header >> PH_VER_SHIFT) & PH_VER_MASK; }
78 | 
79 |   uint8_t getPathHashSize() const { return (path_len >> 6) + 1; }
80 |   uint8_t getPathHashCount() const { return path_len & 63; }
81 |   uint8_t getPathByteLen() const { return getPathHashCount() * getPathHashSize(); }
82 |   void setPathHashCount(uint8_t n) { path_len &= ~63; path_len |= n; }
83 |   void setPathHashSizeAndCount(uint8_t sz, uint8_t n) { path_len = ((sz - 1) << 6) | (n & 63); }
84 | 
85 |   static uint8_t copyPath(uint8_t* dest, const uint8_t* src, uint8_t path_len);  // returns path_len
```

</details>

<details>
<summary><strong>OpenHop Repeater</strong> — <code>repeater/handler_helpers/path.py</code> (L55–L82)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_repeater/blob/dev/repeater/handler_helpers/path.py#L55-L82)

```python
55 |             aes_key = shared_secret[:16]
56 |             decrypted = CryptoUtils.mac_then_decrypt(aes_key, shared_secret, mac_and_data)
57 | 
58 |             if not decrypted:
59 |                 logger.debug(f"Failed to decrypt PATH packet from 0x{src_hash:02X}")
60 |                 return False
61 | 
62 |             # Parse decrypted PATH data
63 |             # Format: path_len(1) + path[path_len] + extra_type(1) + extra[...]
64 |             if len(decrypted) < 1:
65 |                 logger.debug("Decrypted PATH data too short")
66 |                 return False
67 | 
68 |             path_len = decrypted[0]
69 |             if len(decrypted) < 1 + path_len:
70 |                 logger.debug(
71 |                     f"PATH data truncated: need {1 + path_len} bytes, got {len(decrypted)}"
72 |                 )
73 |                 return False
74 | 
75 |             path_data = decrypted[1 : 1 + path_len]
76 | 
77 |             # Update client's out_path (same as C++ memcpy)
78 |             client.out_path = bytearray(path_data)
79 |             client.out_path_len = path_len
80 |             client.last_activity = int(time.time())
81 | 
82 |             logger.info(
```

</details>
