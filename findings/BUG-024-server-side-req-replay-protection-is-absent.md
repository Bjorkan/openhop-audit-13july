# BUG-024 — Server-side REQ replay protection is absent

[← Bug list](../README.md#bug-list)

| Field | Value |
|---|---|
| Severity | **High** |
| Area | Server-side REQ security |
| Affected components | OpenHop Core |
| Status | Confirmed from the supplied source snapshots |

## TL;DR

ProtocolRequestHandler parses the request timestamp but does not compare it with per-client state before running the requested operation. A captured administrative request can be replayed. To fix it, persist a last accepted request timestamp per authenticated client and reproduce MeshCore's strict comparison/update order. The check must be inside the core handler, not optional application code.

**Current status: ✅ Fully fixed.** ProtocolRequestHandler now keeps a per-authenticated-client watermark, rejects timestamps that are not strictly newer, and advances the watermark only after a valid handled request. The complete ordering is in [protocol_request.py](https://github.com/openhop-dev/openhop_core/blob/fix/all-the-things-core/src/openhop_core/node/handlers/protocol_request.py#L155-L183). This fix was introduced in [`bfc0c0d` — `fix(req): reject replayed authenticated client requests`](https://github.com/openhop-dev/openhop_core/commit/bfc0c0d). Rechecked against the supplied branch heads—Core [`9355d08`](https://github.com/openhop-dev/openhop_core/commit/9355d08e21423886a17979c0d8defb891f5d9d72) and Repeater [`6aafa7f`](https://github.com/openhop-dev/openhop_repeater/commit/6aafa7fe991b5b3199b18149f84417f8522d94b2)—and no regression was found in this finding's previously assessed behavior.

## What happens

ProtocolRequestHandler parses the request timestamp but does not compare it with per-client state before running the requested operation. A captured administrative request can be replayed.

## How official MeshCore handles it

The simple repeater accepts REQ only when timestamp is strictly greater than client->last_timestamp, then updates last_timestamp and activity after a valid request.

## How the OpenHop stack handles it

**OpenHop Core:** The handler logs timestamp and immediately calls _handle_request. It neither reads nor writes a replay watermark.

## What needs to change

Persist a last accepted request timestamp per authenticated client and reproduce MeshCore's strict comparison/update order. The check must be inside the core handler, not optional application code.

## Suggested code change

**Important:** The linked code was generated by an LLM. It is not reviewed, tested, or ready to apply directly. It only illustrates the approximate files, control flow, and code changes that may be required. A maintainer must verify the behavior against MeshCore, rewrite it as needed, add tests, and review interactions with other fixes.

- **OpenHop Core:** `src/openhop_core/node/handlers/protocol_request.py`

[Open the suggested patch](../patches/BUG-024.patch)

## Source links

These links point to the branches reviewed for this audit. Line numbers can move after later commits.

| Project | Why it matters | Source |
|---|---|---|
| MeshCore | Reference | [`examples/simple_repeater/MyMesh.cpp` L659–L688](https://github.com/meshcore-dev/MeshCore/blob/main/examples/simple_repeater/MyMesh.cpp#L659-L688) |
| OpenHop Core | Affected implementation | [`src/openhop_core/node/handlers/protocol_request.py` L120–L153](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/node/handlers/protocol_request.py#L120-L153) |
| OpenHop Core | Affected implementation | [`src/openhop_core/node/handlers/protocol_request.py` L240–L325](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/node/handlers/protocol_request.py#L240-L325) |

## Relevant source excerpts

The excerpts are collapsed to keep the report easy to scan.

<details>
<summary><strong>MeshCore</strong> — <code>examples/simple_repeater/MyMesh.cpp</code> (L659–L688)</summary>

[Open the cited range on GitHub](https://github.com/meshcore-dev/MeshCore/blob/main/examples/simple_repeater/MyMesh.cpp#L659-L688)

```cpp
659 |   if (type == PAYLOAD_TYPE_REQ) { // request (from a Known admin client!)
660 |     uint32_t timestamp;
661 |     memcpy(&timestamp, data, 4);
662 | 
663 |     if (timestamp > client->last_timestamp) { // prevent replay attacks
664 |       int reply_len = handleRequest(client, timestamp, &data[4], len - 4);
665 |       if (reply_len == 0) return; // invalid command
666 | 
667 |       client->last_timestamp = timestamp;
668 |       client->last_activity = getRTCClock()->getCurrentTime();
669 | 
670 |       if (packet->isRouteFlood()) {
671 |         // let this sender know path TO here, so they can use sendDirect(), and ALSO encode the response
672 |         mesh::Packet *path = createPathReturn(client->id, secret, packet->path, packet->path_len,
673 |                                               PAYLOAD_TYPE_RESPONSE, reply_data, reply_len);
674 |         if (path) sendFloodReply(path, SERVER_RESPONSE_DELAY, packet->getPathHashSize());
675 |       } else {
676 |         mesh::Packet *reply =
677 |             createDatagram(PAYLOAD_TYPE_RESPONSE, client->id, secret, reply_data, reply_len);
678 |         if (reply) {
679 |           if (client->out_path_len != OUT_PATH_UNKNOWN) { // we have an out_path, so send DIRECT
680 |             sendDirect(reply, client->out_path, client->out_path_len, SERVER_RESPONSE_DELAY);
681 |           } else {
682 |             sendFloodReply(reply, SERVER_RESPONSE_DELAY, packet->getPathHashSize());
683 |           }
684 |         }
685 |       }
686 |     } else {
687 |       MESH_DEBUG_PRINTLN("onPeerDataRecv: possible replay attack detected");
688 |     }
```

</details>

<details>
<summary><strong>OpenHop Core</strong> — <code>src/openhop_core/node/handlers/protocol_request.py</code> (L120–L153)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/node/handlers/protocol_request.py#L120-L153)

```python
120 |                 plaintext = candidate_plaintext
121 |                 break
122 | 
123 |             if client is None or shared_secret is None or plaintext is None:
124 |                 if decrypt_attempted == 0:
125 |                     self.log(f"No shared secret for client 0x{src_hash:02X}")
126 |                 else:
127 |                     self.log(
128 |                         f"Failed to decrypt REQ for all {decrypt_attempted} candidate(s) "
129 |                         f"with src hash 0x{src_hash:02X}"
130 |                     )
131 |                 return None
132 | 
133 |             # Parse request
134 |             if len(plaintext) < 5:
135 |                 self.log("REQ packet too short")
136 |                 return None
137 | 
138 |             timestamp = struct.unpack("<I", plaintext[0:4])[0]
139 |             req_type = plaintext[4]
140 |             req_data = plaintext[5:] if len(plaintext) > 5 else b""
141 | 
142 |             self.log(f"REQ type=0x{req_type:02X}, timestamp={timestamp}")
143 | 
144 |             # Handle request
145 |             response_data = await self._handle_request(
146 |                 client, timestamp, req_type, req_data
147 |             )
148 | 
149 |             if response_data:
150 |                 return self._build_response(
151 |                     packet, client, response_data, shared_secret
152 |                 )
153 | 
```

</details>

<details>
<summary><strong>OpenHop Core</strong> — <code>src/openhop_core/node/handlers/protocol_request.py</code> (L243–L259, L284–L300)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/node/handlers/protocol_request.py#L240-L325)

```python
243 |         """
244 |         Build RESPONSE packet to send back to client.
245 | 
246 |         Matches simple_repeater firmware: REQ via flood → path-return PATH via flood;
247 |         REQ via direct → RESPONSE via direct (if client out_path) else RESPONSE via flood.
248 |         """
249 |         try:
250 |             # Get client identity
251 |             from openhop_core.protocol.identity import Identity
252 | 
253 |             if hasattr(client, "id") and hasattr(client.id, "get_public_key"):
254 |                 client_identity = client.id
255 |             else:
256 |                 pk = (
257 |                     bytes.fromhex(client.public_key)
258 |                     if isinstance(client.public_key, str)
259 |                     else client.public_key
…
284 |                     secret=shared_secret,
285 |                     path=path_list,
286 |                     extra_type=PAYLOAD_TYPE_RESPONSE,
287 |                     extra=response_data,
288 |                     path_len_encoded=path_len_encoded_arg,
289 |                 )
290 |                 hash_size = (
291 |                     PathUtils.get_path_hash_size(path_len_byte)
292 |                     if PathUtils.is_valid_path_len(path_len_byte)
293 |                     else 1
294 |                 )
295 |                 reply_packet.apply_path_hash_mode(hash_size - 1)
296 |                 self.log(f"PATH (path-return) built for 0x{client_hash:02X} via FLOOD")
297 |                 return reply_packet
298 | 
299 |             # REQ via direct: use client out_path if set, else flood
300 |             route_type = "flood"
```

</details>
