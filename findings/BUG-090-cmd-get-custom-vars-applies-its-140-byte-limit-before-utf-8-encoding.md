# BUG-090 — CMD_GET_CUSTOM_VARS applies its 140-byte limit before UTF-8 encoding

[← Active finding list](../README.md#active-findings)

| Field | Value |
|---|---|
| Severity | **Low** |
| Area | Companion custom variables |
| Affected components | OpenHop Core |
| Status | Confirmed from the supplied source snapshots |

## TL;DR

OpenHop slices the custom-variable string to 140 Python characters and encodes afterward. Non-ASCII names or values can therefore produce far more than 140 wire bytes and exceed the response budget the code claims to enforce.

**Current status: 🔴 Not fixed.** Confirmed in the supplied Core snapshot [`9ea7269`](https://github.com/openhop-dev/openhop_core/commit/9ea7269a7e7e903fe433b1f952a4026fe3dcc81b) and Repeater snapshot [`6aafa7f`](https://github.com/openhop-dev/openhop_repeater/commit/6aafa7fe991b5b3199b18149f84417f8522d94b2), as applicable. The complete existing test suites pass, but no implementation or regression test covers this MeshCore compatibility path.

## What happens

OpenHop slices the custom-variable string to 140 Python characters and encodes afterward. Non-ASCII names or values can therefore produce far more than 140 wire bytes and exceed the response budget the code claims to enforce.

## How official MeshCore handles it

MeshCore builds the response directly in a byte buffer and applies its 140-unit threshold to the byte pointer rather than to Unicode code points.

## How the OpenHop stack handles it

A serialized custom-variable string containing 140 two-byte characters yields 280 UTF-8 bytes before the response code, because the limit is applied to Unicode code points rather than the serialized bytes.

## What needs to change

Encode incrementally into a byte buffer capped at 140 bytes. Truncate at a valid UTF-8 boundary or reject oversized variables, and test multibyte names and values.

## Suggested code change

**Important:** The linked code was generated by an LLM. It is not reviewed, tested, or ready to apply directly. It only illustrates the approximate files, control flow, and code changes that may be required. A maintainer must verify the behavior against MeshCore, rewrite it as needed, add tests, and review interactions with other fixes.

[Open the suggested patch](../patches/BUG-090.patch)

## Source links

These links identify the code ranges used for this finding. The local ZIP snapshots listed in `AUDIT-SNAPSHOT.txt` are the audit source of truth.

| Project | Why it matters | Source |
|---|---|---|
| Core | Character-based limit | [`src/openhop_core/companion/frame_server/commands_device.py` L441–L445](https://github.com/openhop-dev/openhop_core/blob/9ea7269a7e7e903fe433b1f952a4026fe3dcc81b/src/openhop_core/companion/frame_server/commands_device.py#L441-L445) |
| MeshCore | Official byte-buffer bound | [`examples/companion_radio/MyMesh.cpp` L1784–L1797](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.cpp#L1784-L1797) |

## Relevant source excerpts

<details>
<summary><strong>Core</strong> — <code>src/openhop_core/companion/frame_server/commands_device.py</code> (L441–L445)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_core/blob/9ea7269a7e7e903fe433b1f952a4026fe3dcc81b/src/openhop_core/companion/frame_server/commands_device.py#L441-L445)

```python
 441 |     async def _cmd_get_custom_vars(self, data: bytes) -> None:
 442 |         custom_vars = self.bridge.get_custom_vars()
 443 |         parts = [f"{k}:{v}" for k, v in custom_vars.items()]
 444 |         csv = ",".join(parts)[:140]
 445 |         self._write_frame(bytes([RESP_CODE_CUSTOM_VARS]) + csv.encode("utf-8", errors="replace"))
```

</details>
<details>
<summary><strong>MeshCore</strong> — <code>examples/companion_radio/MyMesh.cpp</code> (L1784–L1797)</summary>

[Open the cited range on GitHub](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.cpp#L1784-L1797)

```cpp
1784 |   } else if (cmd_frame[0] == CMD_GET_CUSTOM_VARS) {
1785 |     out_frame[0] = RESP_CODE_CUSTOM_VARS;
1786 |     char *dp = (char *)&out_frame[1];
1787 |     for (int i = 0; i < sensors.getNumSettings() && dp - (char *)&out_frame[1] < 140; i++) {
1788 |       if (i > 0) {
1789 |         *dp++ = ',';
1790 |       }
1791 |       strcpy(dp, sensors.getSettingName(i));
1792 |       dp = strchr(dp, 0);
1793 |       *dp++ = ':';
1794 |       strcpy(dp, sensors.getSettingValue(i));
1795 |       dp = strchr(dp, 0);
1796 |     }
1797 |     _serial->writeFrame(out_frame, dp - (char *)out_frame);
```

</details>
