# BUG-052 — Advert sends report success after failure and bypass the repeater engine

[← Bug list](../README.md#bug-list)

| Field | Value |
|---|---|
| Severity | **Medium** |
| Area | Repeater outbound adverts |
| Affected components | OpenHop Repeater |
| Status | Confirmed from the supplied source snapshots |

## TL;DR

Both `send_advert` and the room-server endpoint await `Dispatcher.send_packet` but ignore its Boolean result. They mark the packet as seen, log success, and return `True` even when dispatch returns `False`. To fix it, send adverts through `router.inject_packet` and RepeaterHandler. Check the result, mark packets seen only through the shared send lifecycle, and return and log failure when transmission fails.

**Current status: ✅ Fully fixed.** Repeater and room-server adverts now use router.inject_packet when the router is available, check its Boolean result, and report failure without marking or logging a rejected transmission as successful. See [send_advert](https://github.com/openhop-dev/openhop_repeater/blob/fix/all-the-things/repeater/main.py#L1274-L1288) and [the room-server endpoint](https://github.com/openhop-dev/openhop_repeater/blob/fix/all-the-things/repeater/web/api_endpoints.py#L5980-L6000). This fix was introduced in [`e5d72ea` — `feat: update advert packet sending with error handling and add corresponding tests`](https://github.com/openhop-dev/openhop_repeater/commit/e5d72ea). Rechecked against the supplied branch heads—Core [`9355d08`](https://github.com/openhop-dev/openhop_core/commit/9355d08e21423886a17979c0d8defb891f5d9d72) and Repeater [`6aafa7f`](https://github.com/openhop-dev/openhop_repeater/commit/6aafa7fe991b5b3199b18149f84417f8522d94b2)—and no regression was found in this finding's previously assessed behavior.

## What happens

Both `send_advert` and the room-server endpoint await `Dispatcher.send_packet` but ignore its Boolean result. They mark the packet as seen, log success, and return `True` even when dispatch returns `False`. They also bypass RepeaterHandler duty-cycle accounting, statistics, and scheduling.

## How official MeshCore handles it

MeshCore sends locally originated packets through the same Dispatcher and packet-manager lifecycle and propagates allocation or queue failure to the companion command result.

## How the OpenHop stack handles it

**OpenHop Repeater:** Periodic adverts and REST-triggered adverts can report success without an RF transmission and are not represented in the same airtime and TX state as other locally originated packets.

## What needs to change

Send adverts through `router.inject_packet` and RepeaterHandler. Check the result, mark packets seen only through the shared send lifecycle, and return and log failure when transmission fails.

## Suggested code change

**Important:** The linked code was generated by an LLM. It is not reviewed, tested, or ready to apply directly. It only illustrates the approximate files, control flow, and code changes that may be required. A maintainer must verify the behavior against MeshCore, rewrite it as needed, add tests, and review interactions with other fixes.

- **OpenHop Repeater:** `repeater/main.py`
- **OpenHop Repeater:** `repeater/web/api_endpoints.py`

[Open the suggested patch](../patches/BUG-052.patch)

## Source links

These links point to the branches reviewed for this audit. Line numbers can move after later commits.

| Project | Why it matters | Source |
|---|---|---|
| MeshCore | Reference | [`src/Mesh.cpp` L622–L723](https://github.com/meshcore-dev/MeshCore/blob/main/src/Mesh.cpp#L622-L723) |
| OpenHop Repeater | Affected implementation | [`repeater/main.py` L1130–L1194](https://github.com/openhop-dev/openhop_repeater/blob/dev/repeater/main.py#L1130-L1194) |
| OpenHop Repeater | Affected implementation | [`repeater/web/api_endpoints.py` L4978–L5015](https://github.com/openhop-dev/openhop_repeater/blob/dev/repeater/web/api_endpoints.py#L4978-L5015) |

## Relevant source excerpts

The excerpts are collapsed to keep the report easy to scan.

<details>
<summary><strong>MeshCore</strong> — <code>src/Mesh.cpp</code> (L670–L686, L705–L721)</summary>

[Open the cited range on GitHub](https://github.com/meshcore-dev/MeshCore/blob/main/src/Mesh.cpp#L622-L723)

```cpp
670 |   } else if (packet->getPayloadType() == PAYLOAD_TYPE_ADVERT) {
671 |     pri = 3;   // de-prioritie these
672 |   } else {
673 |     pri = 1;
674 |   }
675 |   sendPacket(packet, pri, delay_millis);
676 | }
677 | 
678 | void Mesh::sendDirect(Packet* packet, const uint8_t* path, uint8_t path_len, uint32_t delay_millis) {
679 |   packet->header &= ~PH_ROUTE_MASK;
680 |   packet->header |= ROUTE_TYPE_DIRECT;
681 | 
682 |   uint8_t pri;
683 |   if (packet->getPayloadType() == PAYLOAD_TYPE_TRACE) {   // TRACE packets are different
684 |     // for TRACE packets, path is appended to end of PAYLOAD. (path is used for SNR's)
685 |     memcpy(&packet->payload[packet->payload_len], path, path_len);  // NOTE: path_len here can be > 64, and NOT in the new scheme
686 |     packet->payload_len += path_len;
…
705 | 
706 |   packet->path_len = 0;  // path_len of zero means Zero Hop
707 | 
708 |   _tables->hasSeen(packet); // mark this packet as already sent in case it is rebroadcast back to us
709 | 
710 |   sendPacket(packet, 0, delay_millis);
711 | }
712 | 
713 | void Mesh::sendZeroHop(Packet* packet, uint16_t* transport_codes, uint32_t delay_millis) {
714 |   packet->header &= ~PH_ROUTE_MASK;
715 |   packet->header |= ROUTE_TYPE_TRANSPORT_DIRECT;
716 |   packet->transport_codes[0] = transport_codes[0];
717 |   packet->transport_codes[1] = transport_codes[1];
718 | 
719 |   packet->path_len = 0;  // path_len of zero means Zero Hop
720 | 
721 |   _tables->hasSeen(packet); // mark this packet as already sent in case it is rebroadcast back to us
```

</details>

<details>
<summary><strong>OpenHop Repeater</strong> — <code>repeater/main.py</code> (L1130–L1138, L1167–L1183)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_repeater/blob/dev/repeater/main.py#L1130-L1194)

```python
1130 |     async def send_advert(self) -> bool:
1131 | 
1132 |         if not self.dispatcher or not self.local_identity:
1133 |             logger.error("Cannot send advert: dispatcher or identity not initialized")
1134 |             return False
1135 | 
1136 |         mode = self.config.get("repeater", {}).get("mode", "forward")
1137 |         if mode == "no_tx":
1138 |             logger.debug("Adverts disabled in no_tx mode")
…
1167 |                 lon=longitude,
1168 |                 feature1=0,
1169 |                 feature2=0,
1170 |                 flags=flags,
1171 |                 route_type="flood",
1172 |             )
1173 | 
1174 |             # Send via dispatcher
1175 |             await self.dispatcher.send_packet(packet, wait_for_ack=False)
1176 | 
1177 |             if self.repeater_handler:
1178 |                 self.repeater_handler.mark_seen(packet)
1179 |                 pkt_hash = packet.calculate_packet_hash().hex()[:16]
1180 |                 self.dispatcher.packet_filter.track_packet(pkt_hash)
1181 |                 logger.debug("Marked own advert as seen in duplicate cache")
1182 | 
1183 |             logger.info(
```

</details>

<details>
<summary><strong>OpenHop Repeater</strong> — <code>repeater/web/api_endpoints.py</code> (L4978–L5015)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_repeater/blob/dev/repeater/web/api_endpoints.py#L4978-L5015)

```python
4978 |             )
4979 | 
4980 |             if not self.daemon_instance or not self.daemon_instance.dispatcher:
4981 |                 logger.error("Cannot send advert: dispatcher not initialized")
4982 |                 return False
4983 | 
4984 |             # Build flags - just use HAS_NAME for room servers
4985 |             flags = ADVERT_FLAG_IS_ROOM_SERVER | ADVERT_FLAG_HAS_NAME
4986 | 
4987 |             packet = PacketBuilder.create_advert(
4988 |                 local_identity=identity,
4989 |                 name=node_name,
4990 |                 lat=latitude,
4991 |                 lon=longitude,
4992 |                 feature1=0,
4993 |                 feature2=0,
4994 |                 flags=flags,
4995 |                 route_type="flood",
4996 |             )
4997 | 
4998 |             # Send via dispatcher
4999 |             await self.daemon_instance.dispatcher.send_packet(packet, wait_for_ack=False)
5000 | 
5001 |             # Mark as seen to prevent re-forwarding
5002 |             if self.daemon_instance.repeater_handler:
5003 |                 self.daemon_instance.repeater_handler.mark_seen(packet)
5004 |                 logger.debug(f"Marked room server advert '{node_name}' as seen in duplicate cache")
5005 | 
5006 |             logger.info(
5007 |                 f"Sent flood advert for room server '{node_name}' at ({latitude:.6f}, {longitude:.6f})"
5008 |             )
5009 |             return True
5010 | 
5011 |         except Exception as e:
5012 |             logger.error(f"Failed to send room server advert: {e}", exc_info=True)
5013 |             return False
5014 | 
5015 |     # ========== ACL (Access Control List) Endpoints ==========
```

</details>
