# LLM-generated implementation sketch only.
# Not reviewed, compiled, tested, or ready to apply.
# Verify against the current OpenHop architecture and official MeshCore.

diff --git a/src/openhop_core/companion/base_callbacks.py b/src/openhop_core/companion/base_callbacks.py
--- a/src/openhop_core/companion/base_callbacks.py
+++ b/src/openhop_core/companion/base_callbacks.py
@@
-    def clear_push_callbacks(self) -> None:
-        for key in self._push_callbacks:
-            self._push_callbacks[key].clear()
+    def remove_push_callback(self, key: str, callback: Callable) -> None:
+        callbacks = self._push_callbacks.get(key, [])
+        self._push_callbacks[key] = [item for item in callbacks if item is not callback]

diff --git a/src/openhop_core/companion/frame_server/push.py b/src/openhop_core/companion/frame_server/push.py
--- a/src/openhop_core/companion/frame_server/push.py
+++ b/src/openhop_core/companion/frame_server/push.py
@@
-        self.bridge.clear_push_callbacks()
+        self._remove_owned_push_callbacks()
         self.bridge.on_message_event(self._on_message_event)
+        self._owned_push_callbacks.append(("message_event", self._on_message_event))
