# LLM-generated implementation sketch only.
# Not reviewed, compiled, tested, or ready to apply.
# Verify against the current OpenHop architecture and official MeshCore.

diff --git a/src/openhop_core/protocol/packet.py b/src/openhop_core/protocol/packet.py
--- a/src/openhop_core/protocol/packet.py
+++ b/src/openhop_core/protocol/packet.py
@@
         self.path = bytearray(data[idx : idx + path_byte_len])
         idx += path_byte_len
+
+        if idx >= data_len:
+            raise ValueError("missing packet payload")
 
         self.payload = bytearray(data[idx:])
