# BUG-015 — Group-text attempt bits are interpreted as message subtypes
#
# LLM-GENERATED IMPLEMENTATION SKETCH — DO NOT APPLY DIRECTLY.
# This code illustrates only the approximate scope and direction of a possible fix.
# It has not been reviewed, compiled, tested, or checked against the other sketches.
# A maintainer must verify MeshCore behavior, rewrite as needed, add tests, and review interactions.
#
# Files:
# - OpenHop Core: src/openhop_core/node/handlers/group_text.py

==============================================================================
FILE: OpenHop Core / src/openhop_core/node/handlers/group_text.py
CURRENT SNAPSHOT (starting near cited line 142):
------------------------------------------------------------------------------
    def _parse_plaintext_message(self, plaintext: bytes) -> Optional[dict]:
        """Parse the decrypted plaintext according to the spec."""
        if len(plaintext) < 5:  # timestamp(4) + flags(1) minimum
            return None

        try:
            timestamp = int.from_bytes(plaintext[:4], "little")
            flags = plaintext[4]
            # Decode and strip trailing null (AES decrypt is block-aligned)
            raw = plaintext[5:].decode("utf-8", errors="replace")
            message_content = raw.rstrip("\x00")

            # Parse message flags according to spec
            message_type = "unknown"

SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
flags = decrypted[4]
if flags & 0xFC:
    return HandlerResult.rejected("invalid group-text flags")
attempt = flags & 0x03
message_bytes = decrypted[5:]
# GRP_TXT has no direct-message subtype or signed-sender prefix.
