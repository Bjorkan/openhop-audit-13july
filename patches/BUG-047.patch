# BUG-047 — Short SET_OTHER_PARAMS frames reset fields they should preserve
#
# LLM-GENERATED IMPLEMENTATION SKETCH — DO NOT APPLY DIRECTLY.
# This code illustrates only the approximate scope and direction of a possible fix.
# It has not been reviewed, compiled, tested, or checked against the other sketches.
# A maintainer must verify MeshCore behavior, rewrite as needed, add tests, and review interactions.
#
# Files:
# - OpenHop Core: src/openhop_core/companion/frame_server/commands_device.py

==============================================================================
FILE: OpenHop Core / src/openhop_core/companion/frame_server/commands_device.py
CURRENT SNAPSHOT (starting near cited line 371):
------------------------------------------------------------------------------
    async def _cmd_set_other_params(self, data: bytes) -> None:
        """Handle CMD_SET_OTHER_PARAMS (0x26). Mirrors MyMesh.cpp:1290-1305."""
        if len(data) < 1:
            self._write_err(ERR_CODE_ILLEGAL_ARG)
            return
        manual_add = data[0]
        telemetry_modes = data[1] if len(data) >= 2 else 0
        advert_loc_policy = data[2] if len(data) >= 3 else 0
        multi_acks = data[3] if len(data) >= 4 else 0
        self.bridge.set_other_params(manual_add, telemetry_modes, advert_loc_policy, multi_acks)
        self._write_ok()

SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
prefs = self.bridge.get_preferences()
if len(data) >= 1:
    prefs.manual_add_contacts = bool(data[0])
if len(data) >= 2:
    prefs.auto_add_contacts = bool(data[1])
if len(data) >= 3:
    prefs.share_location = bool(data[2])
if len(data) >= 4:
    prefs.auto_add_max_hops = data[3]
self.bridge.save_preferences(prefs)
