# BUG-054 — Unsupported radio changes report success for virtual companions
#
# LLM-GENERATED IMPLEMENTATION SKETCH — DO NOT APPLY DIRECTLY.
# This code illustrates only the approximate scope and direction of a possible fix.
# It has not been reviewed, compiled, tested, or checked against the other sketches.
# A maintainer must verify MeshCore behavior, rewrite as needed, add tests, and review interactions.
#
# Files:
# - OpenHop Core: src/openhop_core/companion/base_config.py
# - OpenHop Core: src/openhop_core/companion/frame_server/commands_device.py
# - OpenHop Core: src/openhop_core/companion/companion_radio.py
# - OpenHop Repeater: repeater/companion/bridge.py
# - OpenHop Repeater: repeater/main.py

==============================================================================
FILE: OpenHop Core / src/openhop_core/companion/base_config.py
CURRENT SNAPSHOT (starting near cited line 58):
------------------------------------------------------------------------------
    def set_radio_params(self, freq_hz: int, bw_hz: int, sf: int, cr: int) -> bool:
        """Set radio parameters (frequency, bandwidth, SF, CR)."""
        if not (5 <= sf <= 12):
            raise ValueError(f"Spreading factor out of range: {sf}")
        if not (5 <= cr <= 8):
            raise ValueError(f"Coding rate out of range: {cr}")
        self.prefs.frequency_hz = freq_hz
        self.prefs.bandwidth_hz = bw_hz
        self.prefs.spreading_factor = sf
        self.prefs.coding_rate = cr
        self._save_prefs()
        return True

    def set_tx_power(self, power_dbm: int) -> bool:

SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
class MutationResult(Enum):
    APPLIED = "applied"
    UNSUPPORTED = "unsupported"
    INVALID = "invalid"
    FAILED = "failed"

async def set_radio_params(self, params) -> MutationResult:
    return MutationResult.UNSUPPORTED

==============================================================================
FILE: OpenHop Core / src/openhop_core/companion/frame_server/commands_device.py
CURRENT SNAPSHOT (starting near cited line 279):
------------------------------------------------------------------------------
    async def _cmd_set_radio_params(self, data: bytes) -> None:
        if len(data) < 10:
            self._write_err(ERR_CODE_ILLEGAL_ARG)
            return
        # Frequency in kHz (match firmware self-info; client sends same encoding)
        freq_khz = struct.unpack_from("<I", data, 0)[0]
        bw = struct.unpack_from("<I", data, 4)[0]
        sf = data[8]
        cr = data[9]
        if not (100_000 <= freq_khz <= 2_500_000):
            self._write_err(ERR_CODE_ILLEGAL_ARG)
            return
        if not (7000 <= bw <= 500000):
            self._write_err(ERR_CODE_ILLEGAL_ARG)

SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
result = await self.bridge.set_radio_params(parsed_params)
if result is MutationResult.APPLIED:
    self._write_ok()
elif result is MutationResult.UNSUPPORTED:
    self._write_err(ERR_CODE_UNSUPPORTED_CMD)
else:
    self._write_err(ERR_CODE_ILLEGAL_ARG)

==============================================================================
FILE: OpenHop Core / src/openhop_core/companion/companion_radio.py
CURRENT SNAPSHOT (starting near cited line 209):
------------------------------------------------------------------------------
    def set_radio_params(self, freq_hz: int, bw_hz: int, sf: int, cr: int) -> bool:
        super().set_radio_params(freq_hz, bw_hz, sf, cr)
        if hasattr(self._radio, "configure_radio"):
            try:
                self._radio.configure_radio(
                    frequency=freq_hz,
                    bandwidth=bw_hz,
                    spreading_factor=sf,
                    coding_rate=cr,
                )
                return True
            except Exception as e:
                logger.error("Error configuring radio: %s", e)
                return False

SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
async def set_radio_params(self, params) -> MutationResult:
    if not self.radio.supports_runtime_configuration:
        return MutationResult.UNSUPPORTED
    return MutationResult.APPLIED if await self.radio.apply(params) else MutationResult.FAILED

==============================================================================
FILE: OpenHop Repeater / repeater/companion/bridge.py
CURRENT SNAPSHOT (starting near cited line 55):
------------------------------------------------------------------------------

class RepeaterCompanionBridge(CompanionBridge):
    """CompanionBridge that persists and loads prefs (full NodePrefs) via SQLite JSON blob."""

    def __init__(
        self,
        identity,
        packet_injector: Callable[..., Any],
        node_name: str = "pyMC",
        adv_type: int = 1,
        max_contacts: int = 1000,
        max_channels: int = 40,
        offline_queue_size: int = 512,
        radio_config: Optional[dict] = None,

SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
async def set_radio_params(self, _params) -> MutationResult:
    return MutationResult.UNSUPPORTED

async def set_radio_tx_power(self, _power_dbm) -> MutationResult:
    return MutationResult.UNSUPPORTED

==============================================================================
FILE: OpenHop Repeater / repeater/main.py
CURRENT SNAPSHOT (starting near cited line 600):
------------------------------------------------------------------------------
                bridge = RepeaterCompanionBridge(
                    identity=identity,
                    # Tag the injector with this companion's hash so inject_packet can
                    # skip its own frame server when echoing TX as raw RX (a node never
                    # hears its own transmission).
                    packet_injector=functools.partial(
                        self.router.inject_packet, origin_hash=companion_hash_str
                    ),
                    node_name=node_name,
                    radio_config=radio_config,
                    sqlite_handler=sqlite_handler,
                    companion_hash=companion_hash_str,
                    on_prefs_saved=_make_sync_node_name_to_config(name),
                    **bridge_kwargs,

SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
# Repeater radio administration remains separate from companion commands.
companion_bridge = RepeaterCompanionBridge(
    ..., capabilities=CompanionCapabilities(runtime_radio_mutation=False)
)
