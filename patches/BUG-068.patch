# BUG-068: Timed-out dispatcher ACK waiters are never removed
#
# LLM-generated implementation sketch only. This is not a tested or directly
# applicable patch. Verify and rewrite it against the current repositories.

diff --git a/src/openhop_core/node/dispatcher.py b/src/openhop_core/node/dispatcher.py
@@
     async def wait_for_ack(self, crc, timeout=ACK_TIMEOUT):
         event = self.expect_ack(crc)
         try:
             await asyncio.wait_for(event.wait(), timeout)
             return True
         except asyncio.TimeoutError:
             return False
+        finally:
+            if self._waiting_acks.get(crc) is event:
+                self._waiting_acks.pop(crc, None)
