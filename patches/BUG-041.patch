# BUG-041 — SELF_INFO reports a hardcoded maximum TX power
#
# LLM-GENERATED IMPLEMENTATION SKETCH — DO NOT APPLY DIRECTLY.
# This code illustrates only the approximate scope and direction of a possible fix.
# It has not been reviewed, compiled, tested, or checked against the other sketches.
# A maintainer must verify MeshCore behavior, rewrite as needed, add tests, and review interactions.
#
# Files:
# - OpenHop Core: src/openhop_core/companion/frame_server/commands_device.py
# - OpenHop Repeater: repeater/main.py
# - OpenHop Repeater: repeater/companion/frame_server.py

==============================================================================
FILE: OpenHop Core / src/openhop_core/companion/frame_server/commands_device.py
CURRENT SNAPSHOT (starting near cited line 37):
------------------------------------------------------------------------------
    async def _cmd_app_start(self, data: bytes) -> None:
        if len(data) >= 1:
            self._app_target_ver = data[0]
        prefs = self.bridge.get_self_info()
        pubkey = self.bridge.get_public_key()
        name = prefs.node_name.encode("utf-8", errors="replace")
        lat = int(getattr(prefs, "latitude", 0) * 1e6)
        lon = int(getattr(prefs, "longitude", 0) * 1e6)
        frame = (
            bytes([RESP_CODE_SELF_INFO, ADV_TYPE_CHAT, prefs.tx_power_dbm, 22])
            + pubkey
            + struct.pack("<ii", lat, lon)
            + bytes(
                [

SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
max_tx_power = self.bridge.get_max_tx_power_dbm()
if max_tx_power is None:
    max_tx_power = DEFAULT_MAX_TX_POWER_DBM
max_tx_power = max(-128, min(127, int(max_tx_power)))
out_frame[MAX_TX_POWER_OFFSET] = max_tx_power & 0xFF

==============================================================================
FILE: OpenHop Repeater / repeater/main.py
CURRENT SNAPSHOT (starting near cited line 600):
------------------------------------------------------------------------------
                bridge = RepeaterCompanionBridge(
                    identity=identity,
                    # Tag the injector with this companion's hash so inject_packet can
                    # skip its own frame server when echoing TX as raw RX (a node never
                    # hears its own transmission).
                    packet_injector=functools.partial(
                        self.router.inject_packet, origin_hash=companion_hash_str
                    ),
                    node_name=node_name,
                    radio_config=radio_config,
                    sqlite_handler=sqlite_handler,
                    companion_hash=companion_hash_str,
                    on_prefs_saved=_make_sync_node_name_to_config(name),
                    **bridge_kwargs,

SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
radio_capabilities = RadioCapabilities(
    max_tx_power_dbm=radio_backend.get_max_tx_power_dbm(),
)
companion_server = FrameServer(..., radio_capabilities=radio_capabilities)

==============================================================================
FILE: OpenHop Repeater / repeater/companion/frame_server.py
CURRENT SNAPSHOT (starting near cited line 25):
------------------------------------------------------------------------------
    Constructor signature is intentionally kept compatible with the
    previous monolithic implementation so ``main.py`` call-sites need
    zero changes.
    """

    def __init__(
        self,
        bridge,
        companion_hash: str,
        port: int = 5000,
        bind_address: str = "0.0.0.0",  # nosec B104 - intentional default for LAN reachability
        client_idle_timeout_sec: Optional[int] = 8 * 60 * 60,  # 8 hours
        sqlite_handler=None,
        local_hash: Optional[int] = None,

SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
def get_max_tx_power_dbm(self) -> int | None:
    return self.radio_capabilities.max_tx_power_dbm
