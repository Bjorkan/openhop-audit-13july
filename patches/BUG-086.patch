# BUG-086: CMD_SEND_TRACE_PATH miscomputes multi-byte route completion and timeout hints
#
# LLM-generated implementation sketch only. This is not a tested or directly
# applicable patch. Verify and rewrite it against the current repositories.

diff --git a/src/openhop_core/companion/frame_server/commands_messaging.py b/src/openhop_core/companion/frame_server/commands_messaging.py
@@
-        est_timeout_ms = TRACE_BASE_TIMEOUT_MS + (path_len * TRACE_PER_PATH_BYTE_TIMEOUT_MS)
+        hop_count = path_len // hash_width
+        est_timeout_ms = self.bridge.estimate_direct_timeout_ms(
+            raw_trace_length, hop_count
+        )
         self._write_sent_response(False, tag, est_timeout_ms)
-        if path_bytes and self.local_hash is not None and path_bytes[-1] == self.local_hash:
-            ...
