# BUG-023 — Flood packets bypass MeshCore's reception-quality delay

[← Archived finding list](../README.md#archived-findings)

| Field | Value |
|---|---|
| Severity | **Medium** |
| Area | Radio receive scheduling |
| Affected components | OpenHop Core, OpenHop Repeater |
| Status | Fully fixed in the current supplied snapshots |

## TL;DR

OpenHop originally omitted MeshCore’s optional reception-quality hold and deliberately collected neighbour-score data before enabling routing effects. The current snapshots now implement the official score-based flood hold while retaining the separate observational neighbour-link report. The feature remains disabled by default, matching firmware defaults.

**Current status: ✅ Fully implemented.** The former intentional divergence is no longer present. Core now implements MeshCore’s packet-score calculation, RadioLib-compatible airtime estimate, `calcRxDelay` exponent, 50 ms threshold, 32-second cap, flood-only hold, and—critically—the deduplication check after the hold; see [`dispatcher.py` L390–L520](https://github.com/openhop-dev/openhop_core/blob/41b6201ea2e3cb9b8468b0eb80c9e22fdad4a6c8/src/openhop_core/node/dispatcher.py#L390-L520) and [`packet_utils.py` L46–L113](https://github.com/openhop-dev/openhop_core/blob/41b6201ea2e3cb9b8468b0eb80c9e22fdad4a6c8/src/openhop_core/protocol/packet_utils.py#L46-L113). Repeater wires the disabled-by-default `rx_delay_base` setting into the dispatcher and supports live reload. The observational neighbour-link report remains separate and bounded. Implemented across Core head [`41b6201`](https://github.com/openhop-dev/openhop_core/commit/41b6201ea2e3cb9b8468b0eb80c9e22fdad4a6c8) and Repeater head [`dd6dfce`](https://github.com/openhop-dev/openhop_repeater/commit/dd6dfce9e89fab76967d91e202d8e47217c30474).

## Original observation

Official MeshCore can delay flood processing from a reception-quality score and estimated airtime. The supplied OpenHop Core snapshot processes the first accepted flood immediately after deduplication and does not reproduce that optional queue.

## Resolution history

RightUp initially deferred routing changes while neighbour-link data was collected. The later Core and Repeater commits implemented MeshCore’s reception-quality hold directly, so the report is now archived as fully implemented rather than as an intentional divergence.

## Compatibility assessment

The optional behavior now matches the relevant MeshCore receive-order semantics: delay is calculated only for flood routes, remains disabled at the default base value, and deduplication happens after the hold. Packet serialization and cryptographic behavior are unchanged.

## Historical suggested code change

The linked file is retained only as historical context. It was generated by an LLM before the final implementation and is **not recommended for application**.

[Open the historical sketch](../patches/BUG-023.patch)

## Source links

| Project | Why it matters | Source |
|---|---|---|
| MeshCore | Optional reception-quality delay reference | [`src/Dispatcher.cpp` L198–L256](https://github.com/meshcore-dev/MeshCore/blob/main/src/Dispatcher.cpp#L198-L256) |
| OpenHop Core | Supplied immediate receive flow | [`src/openhop_core/node/dispatcher.py` L387–L468](https://github.com/openhop-dev/openhop_core/blob/9ea7269a7e7e903fe433b1f952a4026fe3dcc81b/src/openhop_core/node/dispatcher.py#L387-L468) |
| OpenHop Repeater | Later observational neighbour-link scoring | [`repeater/neighbour_links.py`](https://github.com/openhop-dev/openhop_repeater/blob/9688fe70e44d85f3cec7de5c77dd804d915a7506/repeater/neighbour_links.py) |
