# BUG-041 — SELF_INFO reports a hardcoded maximum TX power

[← Archived finding list](../README.md#archived-findings)

| Field | Value |
|---|---|
| Severity | **Low** |
| Area | Companion SELF_INFO |
| Affected components | OpenHop Core, OpenHop Repeater |
| Status | Confirmed from the supplied source snapshots |

## TL;DR

Companion SELF_INFO always reports 22 dBm because Core hardcodes the value and Repeater never supplies the active radio limit. Core needs a read-only maximum-power capability, and Repeater must provide the validated limit of its active board and radio backend.

**Current status: ✅ Fully fixed.** SELF_INFO now obtains the maximum TX-power capability through the companion abstraction instead of writing a literal value. CompanionRadio reports its owned backend capability, while Repeater supplies the shared host radio's getter, attribute, configured limit, or the SX1262 limit as appropriate; see [the Repeater capability resolver](https://github.com/openhop-dev/openhop_repeater/blob/6aafa7fe991b5b3199b18149f84417f8522d94b2/repeater/main.py#L671-L709) and [CompanionBridge's host-backed capability](https://github.com/openhop-dev/openhop_core/blob/9355d08e21423886a17979c0d8defb891f5d9d72/src/openhop_core/companion/companion_bridge.py#L320-L371). The fix is present at Core functional commit [`9355d08`](https://github.com/openhop-dev/openhop_core/commit/9355d08e21423886a17979c0d8defb891f5d9d72) and Repeater merge commit [`6aafa7f`](https://github.com/openhop-dev/openhop_repeater/commit/6aafa7fe991b5b3199b18149f84417f8522d94b2). Rechecked against current Core head [`41b6201`](https://github.com/openhop-dev/openhop_core/commit/41b6201ea2e3cb9b8468b0eb80c9e22fdad4a6c8) and Repeater head [`dd6dfce`](https://github.com/openhop-dev/openhop_repeater/commit/dd6dfce9e89fab76967d91e202d8e47217c30474), as applicable. The original correction remains present, the changed callers preserve it, and the complete regression suites found no regression.

## What happens

### OpenHop Core

The core frame server encodes the maximum TX-power byte as the literal value 22 instead of obtaining it from the active device integration.

### OpenHop Repeater

The repeater does not provide backend-specific maximum TX power to the companion frame server, so clients receive the core fallback rather than the actual hardware limit.

## How official MeshCore handles it

### Relevant to OpenHop Core

The maximum-power byte is derived from the board and radio-specific maximum.

### Relevant to OpenHop Repeater

Clients receive the maximum supported power of the active board/radio combination.

## How the OpenHop stack handles it

### OpenHop Core

The generic companion frame server has no read-only provider for maximum TX power, so every backend reports the same value.

### OpenHop Repeater

CompanionFrameServer construction receives statistics and control hooks but no maximum-power capability sourced from the selected radio adapter or board configuration.

## What needs to change

### OpenHop Core

Add an explicit read-only capability method or constructor callback for maximum TX power and use it in SELF_INFO. Validate and clamp the returned protocol value, provide a safe generic default only when no hardware integration exists, and keep this API independent from radio-mutation commands.

### OpenHop Repeater

Resolve maximum TX power from the active radio backend or validated board configuration and supply it through the core read-only capability API. Do not derive it from per-companion preferences. Add mocked backend tests for at least two different maximum values and an unavailable-capability fallback.

## Suggested code change

**Important:** The linked code was generated by an LLM. It is not reviewed, tested, or ready to apply directly. It only illustrates the approximate files, control flow, and code changes that may be required. A maintainer must verify the behavior against MeshCore, rewrite it as needed, add tests, and review interactions with other fixes.

- **OpenHop Core:** `src/openhop_core/companion/frame_server/commands_device.py`
- **OpenHop Repeater:** `repeater/main.py`
- **OpenHop Repeater:** `repeater/companion/frame_server.py`

[Open the suggested patch](../patches/BUG-041.patch)

## Source links

These links point to the branches reviewed for this audit. Line numbers can move after later commits.

| Project | Why it matters | Source |
|---|---|---|
| MeshCore | Reference | [`examples/companion_radio/MyMesh.cpp` L1025–L1065](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.cpp#L1025-L1065) |
| MeshCore | Reference | [`examples/companion_radio/MyMesh.h` L54–L55](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.h#L54-L55) |
| OpenHop Core | Affected implementation | [`src/openhop_core/companion/frame_server/commands_device.py` L37–L70](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/companion/frame_server/commands_device.py#L37-L70) |
| OpenHop Repeater | Affected implementation | [`repeater/main.py` L600–L692](https://github.com/openhop-dev/openhop_repeater/blob/dev/repeater/main.py#L600-L692) |
| OpenHop Repeater | Affected implementation | [`repeater/companion/frame_server.py` L25–L52](https://github.com/openhop-dev/openhop_repeater/blob/dev/repeater/companion/frame_server.py#L25-L52) |

## Relevant source excerpts

The excerpts are collapsed to keep the report easy to scan.

<details>
<summary><strong>MeshCore</strong> — <code>examples/companion_radio/MyMesh.cpp</code> (L1025–L1065)</summary>

[Open the cited range on GitHub](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.cpp#L1025-L1065)

```cpp
1025 |     i += 20;
1026 |     out_frame[i++] = _prefs.client_repeat;   // v9+
1027 |     out_frame[i++] = _prefs.path_hash_mode;  // v10+
1028 |     _serial->writeFrame(out_frame, i);
1029 |   } else if (cmd_frame[0] == CMD_APP_START &&
1030 |              len >= 8) { // sent when app establishes connection, respond with node ID
1031 |     //  cmd_frame[1..7]  reserved future
1032 |     char *app_name = (char *)&cmd_frame[8];
1033 |     cmd_frame[len] = 0; // make app_name null terminated
1034 |     MESH_DEBUG_PRINTLN("App %s connected", app_name);
1035 | 
1036 |     _iter_started = false; // stop any left-over ContactsIterator
1037 |     int i = 0;
1038 |     out_frame[i++] = RESP_CODE_SELF_INFO;
1039 |     out_frame[i++] = ADV_TYPE_CHAT; // what this node Advert identifies as (maybe node's pronouns too?? :-)
1040 |     out_frame[i++] = _prefs.tx_power_dbm;
1041 |     out_frame[i++] = MAX_LORA_TX_POWER;
1042 |     memcpy(&out_frame[i], self_id.pub_key, PUB_KEY_SIZE);
1043 |     i += PUB_KEY_SIZE;
1044 | 
1045 |     int32_t lat, lon;
1046 |     lat = (sensors.node_lat * 1000000.0);
1047 |     lon = (sensors.node_lon * 1000000.0);
1048 |     memcpy(&out_frame[i], &lat, 4);
1049 |     i += 4;
1050 |     memcpy(&out_frame[i], &lon, 4);
1051 |     i += 4;
1052 |     out_frame[i++] = _prefs.multi_acks; // new v7+
1053 |     out_frame[i++] = _prefs.advert_loc_policy;
1054 |     out_frame[i++] = (_prefs.telemetry_mode_env << 4) | (_prefs.telemetry_mode_loc << 2) |
1055 |                      (_prefs.telemetry_mode_base); // v5+
1056 |     out_frame[i++] = _prefs.manual_add_contacts;
1057 | 
1058 |     uint32_t freq = _prefs.freq * 1000;
1059 |     memcpy(&out_frame[i], &freq, 4);
1060 |     i += 4;
1061 |     uint32_t bw = _prefs.bw * 1000;
1062 |     memcpy(&out_frame[i], &bw, 4);
1063 |     i += 4;
1064 |     out_frame[i++] = _prefs.sf;
1065 |     out_frame[i++] = _prefs.cr;
```

</details>

<details>
<summary><strong>MeshCore</strong> — <code>examples/companion_radio/MyMesh.h</code> (L54–L55)</summary>

[Open the cited range on GitHub](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.h#L54-L55)

```cpp
54 | #ifndef MAX_LORA_TX_POWER
55 | #define MAX_LORA_TX_POWER LORA_TX_POWER
```

</details>

<details>
<summary><strong>OpenHop Core</strong> — <code>src/openhop_core/companion/frame_server/commands_device.py</code> (L37–L70)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/companion/frame_server/commands_device.py#L37-L70)

```python
37 |     async def _cmd_app_start(self, data: bytes) -> None:
38 |         if len(data) >= 1:
39 |             self._app_target_ver = data[0]
40 |         prefs = self.bridge.get_self_info()
41 |         pubkey = self.bridge.get_public_key()
42 |         name = prefs.node_name.encode("utf-8", errors="replace")
43 |         lat = int(getattr(prefs, "latitude", 0) * 1e6)
44 |         lon = int(getattr(prefs, "longitude", 0) * 1e6)
45 |         frame = (
46 |             bytes([RESP_CODE_SELF_INFO, ADV_TYPE_CHAT, prefs.tx_power_dbm, 22])
47 |             + pubkey
48 |             + struct.pack("<ii", lat, lon)
49 |             + bytes(
50 |                 [
51 |                     getattr(prefs, "multi_acks", 0),
52 |                     getattr(prefs, "advert_loc_policy", 0),
53 |                 ]
54 |             )
55 |             + bytes(
56 |                 [
57 |                     getattr(prefs, "telemetry_mode_base", 0)
58 |                     | (getattr(prefs, "telemetry_mode_location", 0) << 2)
59 |                 ]
60 |             )
61 |             + bytes([getattr(prefs, "manual_add_contacts", 0)])
62 |             + struct.pack(
63 |                 "<II",
64 |                 prefs.frequency_hz // 1000,
65 |                 prefs.bandwidth_hz,
66 |             )
67 |             + bytes([prefs.spreading_factor, prefs.coding_rate])
68 |             + name
69 |         )
70 |         self._write_frame(frame)
```

</details>

<details>
<summary><strong>OpenHop Repeater</strong> — <code>repeater/main.py</code> (L649–L665, L671–L687)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_repeater/blob/dev/repeater/main.py#L600-L692)

```python
649 |                     retention = getattr(bridge.message_queue, "_max_size", None)
650 |                     if retention != 0:
651 |                         for msg_dict in sqlite_handler.companion_load_messages(
652 |                             companion_hash_str, limit=retention or 100
653 |                         ):
654 |                             from openhop_core.companion.models import QueuedMessage
655 | 
656 |                             sk = msg_dict.get("sender_key", b"")
657 |                             if isinstance(sk, str):
658 |                                 sk = bytes.fromhex(sk)
659 |                             bridge.message_queue.push(
660 |                                 QueuedMessage(
661 |                                     sender_key=sk,
662 |                                     txt_type=msg_dict.get("txt_type", 0),
663 |                                     timestamp=msg_dict.get("timestamp", 0),
664 |                                     text=msg_dict.get("text", ""),
665 |                                     is_channel=bool(msg_dict.get("is_channel", False)),
…
671 |                 # Ensure public channel (0) exists with default key for new companions
672 |                 from repeater.companion.constants import DEFAULT_PUBLIC_CHANNEL_SECRET
673 | 
674 |                 if bridge.get_channel(0) is None:
675 |                     bridge.set_channel(0, "Public", DEFAULT_PUBLIC_CHANNEL_SECRET)
676 | 
677 |                 self.companion_bridges[companion_hash] = bridge
678 | 
679 |                 frame_server = CompanionFrameServer(
680 |                     bridge=bridge,
681 |                     companion_hash=companion_hash_str,
682 |                     port=tcp_port,
683 |                     bind_address=bind_address,
684 |                     client_idle_timeout_sec=client_idle_timeout_sec,
685 |                     sqlite_handler=sqlite_handler,
686 |                     local_hash=self.local_hash,
687 |                     stats_getter=self._get_companion_stats,
```

</details>

<details>
<summary><strong>OpenHop Repeater</strong> — <code>repeater/companion/frame_server.py</code> (L25–L52)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_repeater/blob/dev/repeater/companion/frame_server.py#L25-L52)

```python
25 |     Constructor signature is intentionally kept compatible with the
26 |     previous monolithic implementation so ``main.py`` call-sites need
27 |     zero changes.
28 |     """
29 | 
30 |     def __init__(
31 |         self,
32 |         bridge,
33 |         companion_hash: str,
34 |         port: int = 5000,
35 |         bind_address: str = "0.0.0.0",  # nosec B104 - intentional default for LAN reachability
36 |         client_idle_timeout_sec: Optional[int] = 8 * 60 * 60,  # 8 hours
37 |         sqlite_handler=None,
38 |         local_hash: Optional[int] = None,
39 |         stats_getter=None,
40 |         control_handler=None,
41 |     ):
42 |         super().__init__(
43 |             bridge=bridge,
44 |             companion_hash=companion_hash,
45 |             port=port,
46 |             bind_address=bind_address,
47 |             client_idle_timeout_sec=client_idle_timeout_sec,
48 |             device_model="openHop-Repeater-Companion",
49 |             device_version=None,  # use FIRMWARE_VER_CODE from openhop-core
50 |             build_date="13 Feb 2026",
51 |             local_hash=local_hash,
52 |             stats_getter=stats_getter,
```

</details>
