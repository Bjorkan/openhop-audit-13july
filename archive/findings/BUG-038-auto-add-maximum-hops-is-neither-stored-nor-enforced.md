# BUG-038 — Auto-add maximum hops is neither stored nor enforced

[← Archived finding list](../README.md#archived-findings)

| Field | Value |
|---|---|
| Severity | **Medium** |
| Area | Companion contact policy |
| Affected components | OpenHop Core |
| Status | Confirmed from the supplied source snapshots |

## TL;DR

MeshCore's auto-add configuration has a second byte limiting how many hops away an advert may be before automatic contact creation. OpenHop accepts and returns only the bitmask byte and auto-adds without that distance test. To fix it, add the persisted preference, accept/cap the optional command byte, return it, and reject automatic additions beyond the configured encoded hop count while still handling existing contacts as firmware does.

**Current status: ✅ Fully fixed.** autoadd_max_hops is now a persisted preference, the companion commands read and return the optional byte, and new contacts are rejected when the encoded hop count reaches the configured cap while existing contacts still update. The enforcement is in [base_contacts.py](https://github.com/openhop-dev/openhop_core/blob/fix/all-the-things-core/src/openhop_core/companion/base_contacts.py#L232-L258). This fix was introduced in [`d061503` — `fix(contacts): store and enforce the auto-add maximum-hop limit`](https://github.com/openhop-dev/openhop_core/commit/d061503). Rechecked against the supplied Core snapshot [`9ea7269`](https://github.com/openhop-dev/openhop_core/commit/9ea7269a7e7e903fe433b1f952a4026fe3dcc81b), which is a version-only bump after the audited functional commit [`9355d08`](https://github.com/openhop-dev/openhop_core/commit/9355d08e21423886a17979c0d8defb891f5d9d72), and the supplied Repeater snapshot [`6aafa7f`](https://github.com/openhop-dev/openhop_repeater/commit/6aafa7fe991b5b3199b18149f84417f8522d94b2); no regression was found.

## What happens

MeshCore's auto-add configuration has a second byte limiting how many hops away an advert may be before automatic contact creation. OpenHop accepts and returns only the bitmask byte and auto-adds without that distance test.

## How official MeshCore handles it

CMD_SET_AUTOADD_CONFIG stores autoadd_config and optional autoadd_max_hops capped at 64. GET returns both, and shouldAutoAdd uses the maximum-hop preference.

## How the OpenHop stack handles it

**OpenHop Core:** NodePrefs has no autoadd_max_hops. The frame handlers ignore data[1], return a two-byte frame lacking the value, and _apply_advert_to_stores has no hop cap.

## What needs to change

Add the persisted preference, accept/cap the optional command byte, return it, and reject automatic additions beyond the configured encoded hop count while still handling existing contacts as firmware does.

## Suggested code change

**Important:** The linked code was generated by an LLM. It is not reviewed, tested, or ready to apply directly. It only illustrates the approximate files, control flow, and code changes that may be required. A maintainer must verify the behavior against MeshCore, rewrite it as needed, add tests, and review interactions with other fixes.

- **OpenHop Core:** `src/openhop_core/companion/models.py`
- **OpenHop Core:** `src/openhop_core/companion/frame_server/commands_device.py`

[Open the suggested patch](../patches/BUG-038.patch)

## Source links

These links point to the branches reviewed for this audit. Line numbers can move after later commits.

| Project | Why it matters | Source |
|---|---|---|
| MeshCore | Reference | [`examples/companion_radio/MyMesh.cpp` L323–L331](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.cpp#L323-L331) |
| MeshCore | Reference | [`examples/companion_radio/MyMesh.cpp` L1954–L1966](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.cpp#L1954-L1966) |
| OpenHop Core | Affected implementation | [`src/openhop_core/companion/models.py` L132–L157](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/companion/models.py#L132-L157) |
| OpenHop Core | Affected implementation | [`src/openhop_core/companion/frame_server/commands_device.py` L360–L369](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/companion/frame_server/commands_device.py#L360-L369) |

## Relevant source excerpts

The excerpts are collapsed to keep the report easy to scan.

<details>
<summary><strong>MeshCore</strong> — <code>examples/companion_radio/MyMesh.cpp</code> (L323–L331)</summary>

[Open the cited range on GitHub](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.cpp#L323-L331)

```cpp
323 |   return (_prefs.autoadd_config & type_bit) != 0;
324 | }
325 | 
326 | bool MyMesh::shouldOverwriteWhenFull() const {
327 |   return (_prefs.autoadd_config & AUTO_ADD_OVERWRITE_OLDEST) != 0;
328 | }
329 | 
330 | uint8_t MyMesh::getAutoAddMaxHops() const {
331 |   return _prefs.autoadd_max_hops;
```

</details>

<details>
<summary><strong>MeshCore</strong> — <code>examples/companion_radio/MyMesh.cpp</code> (L1954–L1966)</summary>

[Open the cited range on GitHub](https://github.com/meshcore-dev/MeshCore/blob/main/examples/companion_radio/MyMesh.cpp#L1954-L1966)

```cpp
1954 |   } else if (cmd_frame[0] == CMD_SET_AUTOADD_CONFIG) {
1955 |     _prefs.autoadd_config = cmd_frame[1];
1956 |     if (len >= 3) {
1957 |       _prefs.autoadd_max_hops = min(cmd_frame[2], (uint8_t)64);
1958 |     }
1959 |     savePrefs();
1960 |     writeOKFrame();
1961 |   } else if (cmd_frame[0] == CMD_GET_AUTOADD_CONFIG) {
1962 |     int i = 0;
1963 |     out_frame[i++] = RESP_CODE_AUTOADD_CONFIG;
1964 |     out_frame[i++] = _prefs.autoadd_config;
1965 |     out_frame[i++] = _prefs.autoadd_max_hops;
1966 |     _serial->writeFrame(out_frame, i);
```

</details>

<details>
<summary><strong>OpenHop Core</strong> — <code>src/openhop_core/companion/models.py</code> (L132–L157)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/companion/models.py#L132-L157)

```python
132 | class NodePrefs:
133 |     """Node configuration preferences (equivalent to firmware NodePrefs)."""
134 | 
135 |     node_name: str = "pyMC"
136 |     adv_type: int = 1  # ADV_TYPE_CHAT
137 |     tx_power_dbm: int = 20
138 |     frequency_hz: int = 915000000
139 |     bandwidth_hz: int = 250000
140 |     spreading_factor: int = 10
141 |     coding_rate: int = 5
142 |     latitude: float = 0.0
143 |     longitude: float = 0.0
144 |     advert_loc_policy: int = 0  # ADVERT_LOC_NONE
145 |     multi_acks: int = 0
146 |     telemetry_mode_base: int = 0  # TELEM_MODE_DENY
147 |     telemetry_mode_location: int = 0
148 |     telemetry_mode_environment: int = 0
149 |     manual_add_contacts: int = 0
150 |     autoadd_config: int = 0
151 |     rx_delay_base: float = 0.0
152 |     airtime_factor: float = 0.0
153 |     # Reported in CMD_DEVICE_QUERY device info frame (byte 80).
154 |     client_repeat: int = 0
155 |     path_hash_mode: int = 0  # 0=1-byte, 1=2-byte, 2=3-byte hashes
156 |     default_scope_name: str = ""
157 |     default_scope_key: bytes = b""
```

</details>

<details>
<summary><strong>OpenHop Core</strong> — <code>src/openhop_core/companion/frame_server/commands_device.py</code> (L360–L369)</summary>

[Open the cited range on GitHub](https://github.com/openhop-dev/openhop_core/blob/dev/src/openhop_core/companion/frame_server/commands_device.py#L360-L369)

```python
360 |     async def _cmd_set_autoadd_config(self, data: bytes) -> None:
361 |         if len(data) < 1:
362 |             self._write_err(ERR_CODE_ILLEGAL_ARG)
363 |             return
364 |         self.bridge.set_autoadd_config(data[0])
365 |         self._write_ok()
366 | 
367 |     async def _cmd_get_autoadd_config(self, data: bytes) -> None:
368 |         config = self.bridge.get_autoadd_config()
369 |         self._write_frame(bytes([RESP_CODE_AUTOADD_CONFIG, config & 0xFF]))
```

</details>
