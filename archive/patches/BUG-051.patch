# BUG-051 — Loop thresholds ignore path-hash width
#
# LLM-GENERATED IMPLEMENTATION SKETCH — DO NOT APPLY DIRECTLY.
# This code illustrates only the approximate scope and direction of a possible fix.
# It has not been reviewed, compiled, tested, or checked against the other sketches.
# A maintainer must verify MeshCore behavior, rewrite as needed, add tests, and review interactions.
#
# Files:
# - OpenHop Repeater: repeater/engine.py

==============================================================================
FILE: OpenHop Repeater / repeater/engine.py
CURRENT SNAPSHOT (starting near cited line 37):
------------------------------------------------------------------------------
# Thresholds for flood loop detection (hash-size-aware: 1, 2, or 3 bytes per hop).
# Count how many times our own hash already exists in the incoming FLOOD path.
# If occurrences >= threshold, treat as loop and drop.
LOOP_DETECT_MAX_COUNTERS = {
    LOOP_DETECT_MINIMAL: 4,
    LOOP_DETECT_MODERATE: 2,
    LOOP_DETECT_STRICT: 1,
}

SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
LOOP_THRESHOLDS = {
    MODE_REPEATER: {1: 4, 2: 3, 3: 2},
    MODE_ROOM: {1: 6, 2: 4, 3: 3},
}

hash_width = packet.path_hash_size
if hash_width == 4:
    return LoopDecision.INVALID_PATH_WIDTH
threshold = LOOP_THRESHOLDS[self.mode][hash_width]
return LoopDecision.DROP if counter >= threshold else LoopDecision.FORWARD
