# BUG-010 — Text attempt numbers above three are discarded
#
# LLM-GENERATED IMPLEMENTATION SKETCH — DO NOT APPLY DIRECTLY.
# This code illustrates only the approximate scope and direction of a possible fix.
# It has not been reviewed, compiled, tested, or checked against the other sketches.
# A maintainer must verify MeshCore behavior, rewrite as needed, add tests, and review interactions.
#
# Files:
# - OpenHop Core: src/openhop_core/protocol/packet_builder.py

==============================================================================
FILE: OpenHop Core / src/openhop_core/protocol/packet_builder.py
CURRENT SNAPSHOT (starting near cited line 935):
------------------------------------------------------------------------------
    @staticmethod
    def create_text_message(
        contact: Any,
        local_identity: LocalIdentity,
        message: str,
        attempt: int = 0,
        message_type: str = "direct",
        out_path: Optional[list] = None,
        txt_type: int = 0,
        timestamp: Optional[int] = None,
    ) -> tuple[Packet, int]:
        """
        Create a secure text message with encryption and CRC validation.


SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
original_attempt = max(0, min(int(attempt), 0xFF))
flags_byte = (txt_type << 2) | (original_attempt & 0x03)
message_bytes = message.encode("utf-8")
if original_attempt > 3:
    message_bytes += b"\x00" + bytes([original_attempt])
plaintext = struct.pack("<IB", timestamp, flags_byte) + signed_sender_prefix + message_bytes
