# BUG-032 — Encoded contact path length is parsed as signed
#
# LLM-GENERATED IMPLEMENTATION SKETCH — DO NOT APPLY DIRECTLY.
# This code illustrates only the approximate scope and direction of a possible fix.
# It has not been reviewed, compiled, tested, or checked against the other sketches.
# A maintainer must verify MeshCore behavior, rewrite as needed, add tests, and review interactions.
#
# Files:
# - OpenHop Core: src/openhop_core/companion/frame_server/commands_contacts.py

==============================================================================
FILE: OpenHop Core / src/openhop_core/companion/frame_server/commands_contacts.py
CURRENT SNAPSHOT (starting near cited line 64):
------------------------------------------------------------------------------
    async def _cmd_add_update_contact(self, data: bytes) -> None:
        if len(data) < 36:
            self._write_err(ERR_CODE_ILLEGAL_ARG)
            return
        pubkey = data[0:PUB_KEY_SIZE]
        adv_type = data[32]
        flags = data[33]
        out_path_len = struct.unpack_from("<b", data, 34)[0]
        out_path_end = 35 + MAX_PATH_SIZE
        if len(data) >= out_path_end:
            out_path = data[35:out_path_end].rstrip(b"\x00")
        else:
            out_path = data[35 : len(data)].rstrip(b"\x00") if len(data) > 35 else b""
        name_start = 35 + MAX_PATH_SIZE

SUGGESTED AFTER-STATE:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
path_len_wire = data[path_len_offset]  # unsigned 0..255
path_len = OUT_PATH_UNKNOWN if path_len_wire == 0xFF else path_len_wire
if path_len != OUT_PATH_UNKNOWN and not PathUtils.is_valid_path_len(path_len):
    self._write_err(ERR_CODE_ILLEGAL_ARG)
    return
